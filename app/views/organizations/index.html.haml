%h3 Find your Organization

%div.row
	%div.span3
		= form_tag '/organizations/' do
			= label_tag(:name, "Search by Organization Name")
			= autocomplete_field_tag 'name', '', autocomplete_organization_name_organizations_path, :update_elements => {:id => '#id_element', :subdomain => '#subdomain'}
			= submit_tag("Search")
	%div.span3
		= form_tag do
			= label_tag(:location, "Search by Organization Location") 
			= text_field_tag(:location)
			= submit_tag("Search")
	%div.span3	
		%p Not finding what you're looking for?

